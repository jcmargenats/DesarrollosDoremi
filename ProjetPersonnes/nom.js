//  Functions pour l'objet nom
function Nom(nom1, nom2, nomfam, init, traitem, nomemp) {
  (this.nom1 = nom1),
    (this.nom2 = nom2),
    (this.nomfam = nomfam),
    (this.init = init),
    (this.traitem = traitem),
    (this.nomemp = nomemp);
}
